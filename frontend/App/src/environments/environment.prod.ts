// Production environment configuration  
export const environment = {
  production: true,
  apiUrl: '/api',  // Production API base URL (will be /eventified/api after base href)
  name: 'production'
};